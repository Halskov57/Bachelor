spring.application.name=application
spring.data.mongodb.uri=mongodb://admin:secret@localhost:27017/projectmanagement?authSource=admin&authMechanism=SCRAM-SHA-256
server.port=8081
JWT_SECRET=jonasognicolai2026@@@verysecuresecretkey@@@

# CORS Configuration - Development includes localhost:3000, Production uses Railway URL
app.cors.allowed-origins=http://localhost:3000,http://127.0.0.1:3000,https://frontend-production-ded6.up.railway.app,https://www.auprj.dk

# GraphQL Configuration  
spring.graphql.path=/graphql

# Disable automatic GraphQL WebSocket to avoid SessionInfo issues
spring.graphql.websocket.enabled=false

# We'll handle WebSocket subscriptions through a simpler approach
# until Spring GraphQL WebSocket transport is fixed