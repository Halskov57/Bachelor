# Use the MongoDB URI from environment variable
spring.data.mongodb.uri=${MONGO_URL}

# Ensure server port matches Dockerfile EXPOSE
server.port=8080

# CORS Configuration for Docker/Production
app.cors.allowed-origins=https://frontend-production-ded6.up.railway.app
